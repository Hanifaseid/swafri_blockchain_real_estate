import { MarketplaceDiscovery } from '@/components/MarketplaceDiscovery';

// Browse Properties — public marketplace discovery page.
// Accessible to all users (unauthenticated, tenants, owners).

export default function PropertiesPage() {
  return <MarketplaceDiscovery />;
}
